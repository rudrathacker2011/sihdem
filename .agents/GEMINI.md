# Custom Agent Rules

* Always remind the user of any manual steps needed, such as replacing the `[YOUR-PASSWORD]` placeholder in `DATABASE_URL` or configuration files, whenever database credentials or environments are set up.
